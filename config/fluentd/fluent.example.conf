# Minimal Fluentd configuration
<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type stdout
</match>

<buffer>
  @type memory
  chunk_limit_size 1m
  total_limit_size 10m
</buffer>
